/*
Custom Function: jsonValuesToString()
Parameters: ~array, ~separator
========================
Converts JSON Values to text with the specified separator
Useful to convert a simple JSON array to text

Notes:

Dependencies: none

Benedick Miller
*/

Let ([

  ~list = JSONListValues ( ~array ; "" ) 

];

  Case (
    ValueCount ( ~list ) ;
    Substitute ( ~list ; ¶ ; ~separator ) ;
    ""
  )
)
